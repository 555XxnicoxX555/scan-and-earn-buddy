{
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 8080",
    "build": "mkdir -p dist && cp index.html styles.css app.js dist/"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.4.8"
  }
}